{
  "dependencies": {
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.19.0"
  },
  "scripts": {
    "make-model": ">npx sequelize-cli model:generate --name User --attributes name:string,password:string,email:string,age:number,address:string",
    "migrate": "npx sequelize-cli db:migrate",
    "undo-migrate": "npx sequelize-cli db:migrate:undo",
    "make-seed": "npx sequelize-cli seed:generate --name demo-user",
    "start": "node index.js"
  },
  "devDependencies": {
    "sequelize-cli": "^6.4.1"
  }
}
